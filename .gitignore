# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local
.env.development.local
.env.test.local
.env.production.local   
.vercel
# Build outputs
dev-dist/
dist/
*.log
node_modules

# IDE / OS
.cursor/

# Supabase CLI cache (canonical + nested under web)
platform/supabase/.temp/
platform/web/supabase/.temp/

# Android (Capacitor native project next to platform/web)
platform/android/app/build/
platform/android/.gradle/
platform/android/captures/
platform/android/app/google-services.json

# Legacy duplicate app trees at repo root (do not re-commit after migration)
/android/
/src/
/public/
/scripts/
/web/

# Android / Java signing keys (never commit; rotate any key that was ever pushed)
*.keystore
*.jks

# Never commit Firebase Admin / service account JSON (server secrets)
*firebase-adminsdk*.json

# Local-only webhook / secret notes (never commit)
chat notify webhook secret.txt

# Word lock files
~$*
