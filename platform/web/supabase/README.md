# Deprecated — use `platform/supabase`

Database migrations and Edge Functions live in:

```
../../supabase/
```

(from this folder: `platform/supabase`)

Deploy functions and push migrations from `platform/supabase`, not from `platform/web/supabase`.
